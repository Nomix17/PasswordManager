export const passwordInputSelectorQueury: string = `
  input[type='password'],
  input[id*='pass'],
  input[name*='pass'],
  input[class*='pass'],
  input[placeholder*='Password']
`;

export const userNameInputElSelectorQueury: string = `
  input[type='text'],
  input[type='email'],
  input[id*='user'],
  input[id*='email'],
  input[name*='user'],
  input[name*='email'],
  input[class*='user'],
  input[class*='email'],
  input[placeholder*='Username'],
  input[placeholder*='Email']
`;

export const submitBtnElSelectorQueuery: string = `
  button[type='submit'],
  input[type='submit'],
  button[id*='submit'],
  input[id*='submit'],
  button[name*='submit'],
  input[name*='submit'],
  button[class*='submit'],
  input[class*='submit'],
  button[aria-label*='submit'],
  input[aria-label*='submit']
`;
